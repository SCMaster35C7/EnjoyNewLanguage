<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="global.sesoc.Youtube.dao.EducationMapper">
	<select id="selectAllEduList" resultType="Education">
		select
			videonum
			, title
			, url
			, originalfile
			, savedfile
			, to_char(regdate, 'YYYY-MM-DD') regdate
			, hitcount
			, recommendation
			, decommendation
		from
			EducationVideo
		order by 
			regdate desc
	</select>
	<select id="selectOneFromEduVideo" parameterType="int" resultType="Education">
		select
			videonum
			, title
			, url
			, originalfile
			, savedfile
			, to_char(regdate, 'YYYY-MM-DD') regdate
			, hitcount
			, recommendation
			, decommendation
		from
			EducationVideo
		where
			videonum = #{videoNum}
	</select>
	<insert id="insertEduVideo" parameterType="Education">
		insert into EducationVideo(
			videonum
			, title
			, url
			, originalfile
			, savedfile
		)values (
			EDUCATION_VIDEO_SEQ.nextval
			, #{title}
			, #{url}
			, #{originalfile}
			, #{savedfile}
		)
	</insert>
</mapper>
